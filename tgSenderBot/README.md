# 🤖 Telegram Message Sender Bot (aiogram)

## Описание
Скрипт читает текст из файла `.txt` и отправляет его в указанный Telegram-чат через бота.
Использует современную библиотеку **aiogram 3.x**.

## Быстрый старт

### Шаг 1: Установка зависимостей
```bash
pip install aiogram
```

### Шаг 2: Создание бота в Telegram

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: `My Sender Bot`)
4. Придумайте username (должен заканчиваться на `bot`, например: `my_sender_bot`)
5. **Скопируйте токен** (выглядит примерно так: `7468299405:AAHSvxPQv8FO2npwCXBhPVqufMU0aGOrkik`)

### Шаг 3: Настройка скрипта

Откройте файл `main.py` и вставьте ваш токен:

```python
# Токен вашего бота
BOT_TOKEN = "7468299405:AAHSvxPQv8FO2npwCXBhPVqufMU0aGOrkik"
```

### Шаг 4: Получение Chat ID

**Способ 1 - Автоматически через скрипт (рекомендуется):**

1. Найдите вашего бота в Telegram по username
2. Нажмите **START** и отправьте любое сообщение
3. В файле `main.py` раскомментируйте строку:
```python
asyncio.run(get_chat_id(BOT_TOKEN))
```
4. Закомментируйте строку:
```python
# asyncio.run(main())
```
5. Запустите скрипт:
```bash
python main.py.py
```
6. Скрипт покажет ваш Chat ID, например: `123456789`
7. Скопируйте Chat ID и вставьте в скрипт:
```python
CHAT_ID = "123456789"
```

**Способ 2 - Вручную через браузер:**
1. Отправьте боту `/start` в Telegram
2. Откройте в браузере: 
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```
3. Найдите `"chat":{"id":123456789}` - это ваш Chat ID

**Для групповых чатов:**
1. Добавьте бота в группу
2. Дайте боту права администратора (или хотя бы отправку сообщений)
3. Отправьте в группу любое сообщение
4. Используйте функцию `get_chat_id()` - она покажет ID группы

### Шаг 5: Создание файла с текстом

Создайте файл `message.txt` в той же папке со скриптом:

```
Привет! Это тестовое сообщение из файла.
Можно писать многострочный текст.
Эмодзи тоже работают 🚀
```

### Шаг 6: Запуск

После настройки BOT_TOKEN и CHAT_ID:

1. Убедитесь, что раскомментирована строка `asyncio.run(main())`
2. Запустите:
```bash
python main.py
```

## Полная инструкция по настройке

### 1. Первый запуск (получение Chat ID)

```python
# В конце файла main.py.py:
asyncio.run(get_chat_id(BOT_TOKEN))  # Раскомментировать
# asyncio.run(main())  # Закомментировать
```

Вывод будет примерно таким:
```
🤖 Информация о боте:
   Имя: My Sender Bot
   Username: @my_sender_bot
   ID: 7468299405

📋 Доступные чаты:
============================================================

💬 Chat ID: 123456789
   Тип: private
   Username: @your_username
   Имя: Your Name
------------------------------------------------------------

✅ Найдено чатов: 1
```

### 2. Обычная работа (отправка сообщений)

```python
# В конце файла main.py.py:
# asyncio.run(get_chat_id(BOT_TOKEN))  # Закомментировать
asyncio.run(main())  # Раскомментировать
```

## Примеры использования

### Отправка простого сообщения
```bash
# Создайте message.txt с текстом
echo "Тестовое сообщение" > message.txt

# Запустите скрипт
python main.py.py
```

### Отправка длинного текста
Скрипт автоматически разбивает сообщения длиннее 4096 символов на части.

### Отправка из Python-кода
```python
import asyncio
from main.py import send_message_from_file

async def send_notification():
    success = await send_message_from_file(
        bot_token="YOUR_BOT_TOKEN",
        chat_id="YOUR_CHAT_ID",
        file_path="notification.txt"
    )
    if success:
        print("Уведомление отправлено!")

asyncio.run(send_notification())
```

## Структура файлов проекта

```
project/
├── main.py              # Основной скрипт
├── message.txt          # Файл с текстом для отправки
└── README.md            # Эта инструкция
```

## Требования
- Python 3.7+
- aiogram 3.x

## Дополнительные возможности

### Изменить путь к файлу
```python
TEXT_FILE = "data/notifications/message.txt"
```

### Отправка с форматированием (Markdown)
```python
from aiogram.enums import ParseMode

await bot.send_message(
    chat_id=chat_id,
    text="*Жирный текст* _курсив_ `код`",
    parse_mode=ParseMode.MARKDOWN
)
```

### Отправка с HTML-форматированием
```python
from aiogram.enums import ParseMode

await bot.send_message(
    chat_id=chat_id,
    text="<b>Жирный</b> <i>курсив</i> <code>код</code>",
    parse_mode=ParseMode.HTML
)
